version: '3.4'
services:
  api:
    image: mmazurovsky/postraves-wiki-api
    build:
      context: .
      dockerfile: Dockerfile
      target: final
#    env_file:
#      - ../.env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
