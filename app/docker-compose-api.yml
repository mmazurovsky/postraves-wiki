version: '3.4'
services:
  api:
    image: ${WIKI_API_IMAGE}
    build:
      context: .
      dockerfile: Dockerfile
      target: final
    env_file:
      - ${WIKI_API_ENV_LOCATION}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
